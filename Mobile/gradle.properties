org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
android.nonTransitiveRClass=true
# JDK is auto-detected from Android Studio / JAVA_HOME (portable across Windows/Linux/macOS).
# Do NOT hardcode org.gradle.java.home to an OS-specific path — it breaks builds on other machines.
android.overridePathCheck=true
